# Privacy And Data Handling

Codex Sync Kit stores selected local Codex files in a user-controlled private GitHub repository. It does not operate a service and does not send data to a project-owned backend.

The user controls profile selection. `safe` is the default. `full` and `custom` can include more files, but risky paths require explicit confirmation.
